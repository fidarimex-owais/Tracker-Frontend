import { Link } from 'react-router-dom';
import { useAuth } from '../../auth/useAuth';
export default function SupervisorDashboard(){ const {user}=useAuth(); return <section className="space-y-6"><div><p className="text-sm font-semibold text-blue-700">SUPERVISOR</p><h2 className="text-3xl font-bold text-slate-900">Dashboard</h2><p className="mt-2 text-slate-500">Welcome, {user.email}. Use the scanner to verify QR and barcode records.</p></div><Link to="/supervisor/scanner" className="block max-w-lg rounded-xl border border-slate-200 bg-white p-6 shadow-sm hover:shadow-md"><h3 className="text-xl font-bold text-slate-900">QR / Barcode Lookup</h3><p className="mt-2 text-sm leading-6 text-slate-500">Paste a scanned QR unique ID or barcode ID to retrieve the matching package, line, farmer, location, and hand category.</p><p className="mt-4 font-semibold text-blue-800">Open scanner →</p></Link></section>; }
